{erl_opts, [debug_info]}.
%% rabbit_common is used only for headers (#auth_user) + the authn behaviour,
%% which are stable across RabbitMQ 3.8-4.0; the pin just fixes the build.
{deps, [{rabbit_common, "3.12.14"}]}.
